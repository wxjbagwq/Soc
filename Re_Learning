1. 裸板调用C函数：
(1) 软件初始化：栈(准备指向内存才能使用内存)，C函数的返回地址，调用main
(2) 硬件初始化: 关看门狗(看门狗在上电会检测有没有操作系统，没有的话会重启，所以要关闭)，初始化时钟(2440刚刚上电只有12Mhz，而最大时钟为400Mhz),初始化内存
(3) 所以裸板程序U-boot也要做上述的操作！！
2. 链接地址：程序运行时候的位置！区分代码保存的地方。









